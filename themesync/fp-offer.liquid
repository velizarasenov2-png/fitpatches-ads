{%- comment -%} FitPatches — Оферта: бъндъл избор + бърза поръчка с Purchase pixel {%- endcomment -%}
{%- liquid
  assign cur = shop.currency
  assign variant_id = product.selected_or_first_available_variant.id
  assign pixel_id = section.settings.pixel_id | default: '1051819453965625'
  assign endpoint = section.settings.cloud_endpoint
  assign content_id = 'fitpatches'
  assign packs_list = '1,2,3' | split: ','
  assign patches_list = '30,60,90' | split: ','
  assign months_list = '1,2,3' | split: ','
  assign price_list = '14.99,24.99,32.99' | split: ','
  assign perpack_list = '14.99,12.50,11.00' | split: ','
  assign perday_list = '0.50,0.42,0.37' | split: ','
  assign save_list = 'x,Спести 5 лв,Спести 12 лв' | split: ','
  assign badge_list = 'x,x,Най-популярен' | split: ','
  assign default_tier = 3
-%}

<section
  class="fp-offer"
  id="fp-offer-{{ section.id }}"
  data-pixel="{{ pixel_id }}"
  data-endpoint="{{ endpoint }}"
  data-variant="{{ variant_id }}"
  data-content-id="{{ content_id }}"
  data-currency="{{ cur }}">

  <div class="fp-offer__wrap">
    <h2 class="fp-offer__title">Колко пакета желаете</h2>
    <p class="fp-offer__sub">Повечето клиенти избират 2 или 3 пакета за пълен резултат.</p>

    <div class="fp-offer__tiers" role="radiogroup" aria-label="Изберете пакет">
      {%- for packs in packs_list -%}
        {%- assign i = forloop.index0 -%}
        {%- assign idx = forloop.index -%}
        {%- assign patches = patches_list[i] -%}
        {%- assign months = months_list[i] -%}
        {%- assign total = price_list[i] -%}
        {%- assign per_pack = perpack_list[i] -%}
        {%- assign per_day = perday_list[i] -%}
        {%- assign save = save_list[i] -%}
        {%- assign badge = badge_list[i] -%}
        <label class="fp-tier{% if idx == default_tier %} is-selected{% endif %}" data-tier="{{ idx }}">
          <input type="radio" name="fp-tier-{{ section.id }}" value="{{ idx }}" data-packs="{{ packs }}" data-total="{{ total }}"{% if idx == default_tier %} checked{% endif %}>
          {%- unless badge == 'x' -%}<span class="fp-tier__badge">{{ badge }}</span>{%- endunless -%}
          <span class="fp-tier__radio" aria-hidden="true"></span>
          <span class="fp-tier__body">
            <span class="fp-tier__name">{{ packs }}{% if packs == '1' %} пакет{% else %} пакета{% endif %}</span>
            <span class="fp-tier__meta">{{ patches }} лепенки · {{ months }}{% if months == '1' %} месец{% else %} месеца{% endif %}</span>
            <span class="fp-tier__price">
              <span class="fp-tier__total">{{ total }} {{ cur }}</span>
              {%- unless packs == '1' -%}<span class="fp-tier__unit">{{ per_pack }} {{ cur }} / пакет</span>{%- endunless -%}
            </span>
            <span class="fp-tier__perday">{{ per_day }} {{ cur }} на ден</span>
          </span>
          {%- unless save == 'x' -%}<span class="fp-tier__save">{{ save }}</span>{%- endunless -%}
        </label>
      {%- endfor -%}
    </div>

    <div class="fp-offer__actions">
      <button type="button" class="fp-btn fp-btn--order" data-open-order>Поръчай с наложен платеж</button>
      <button type="button" class="fp-btn fp-btn--cart" data-add-cart>Добави в количката</button>
    </div>

    <p class="fp-offer__reassure">Плащане при доставка · Доставка до 2–4 работни дни · Без предплащане</p>

    <form class="fp-order" data-order-form hidden>
      <p class="fp-order__lead">Оставете име и телефон — ще Ви се обадим за потвърждение на поръчката.</p>
      <div class="fp-order__row"><input type="text" name="fp_name" placeholder="Вашето име" autocomplete="name" required></div>
      <div class="fp-order__row"><input type="tel" name="fp_phone" placeholder="Телефон (напр. 0888 123 456)" autocomplete="tel" inputmode="tel" required></div>
      <div class="fp-order__summary" data-order-summary></div>
      <button type="submit" class="fp-btn fp-btn--submit">Потвърди поръчката</button>
      <p class="fp-order__err" data-order-err hidden></p>
    </form>

    <div class="fp-order__success" data-order-success hidden>
      <strong>Благодарим! Поръчката е приета.</strong>
      <span>Ще Ви се обадим скоро за потвърждение.</span>
    </div>
  </div>
</section>

<style>
  #fp-offer-{{ section.id }}{--fp-accent:#1e874b;--fp-accent-soft:#e8f5ee}
  #fp-offer-{{ section.id }} .fp-offer__wrap{max-width:640px;margin:0 auto;padding:24px 16px}
  #fp-offer-{{ section.id }} .fp-offer__title{font-size:22px;font-weight:800;text-align:center;margin:0 0 4px}
  #fp-offer-{{ section.id }} .fp-offer__sub{text-align:center;color:#666;margin:0 0 18px;font-size:14px}
  #fp-offer-{{ section.id }} .fp-offer__tiers{display:flex;flex-direction:column;gap:12px}
  #fp-offer-{{ section.id }} .fp-tier{position:relative;display:flex;align-items:center;gap:14px;border:2px solid #e3e3e3;border-radius:14px;padding:16px;cursor:pointer;transition:border-color .15s,box-shadow .15s;background:#fff}
  #fp-offer-{{ section.id }} .fp-tier.is-selected{border-color:var(--fp-accent);box-shadow:0 0 0 3px var(--fp-accent-soft)}
  #fp-offer-{{ section.id }} .fp-tier input{position:absolute;opacity:0;pointer-events:none}
  #fp-offer-{{ section.id }} .fp-tier__radio{flex:0 0 22px;width:22px;height:22px;border:2px solid #cfcfcf;border-radius:50%;position:relative}
  #fp-offer-{{ section.id }} .fp-tier.is-selected .fp-tier__radio{border-color:var(--fp-accent)}
  #fp-offer-{{ section.id }} .fp-tier.is-selected .fp-tier__radio:after{content:"";position:absolute;inset:4px;border-radius:50%;background:var(--fp-accent)}
  #fp-offer-{{ section.id }} .fp-tier__body{display:flex;flex-direction:column;gap:2px;flex:1}
  #fp-offer-{{ section.id }} .fp-tier__name{font-weight:800;font-size:16px}
  #fp-offer-{{ section.id }} .fp-tier__meta{color:#777;font-size:13px}
  #fp-offer-{{ section.id }} .fp-tier__price{display:flex;align-items:baseline;gap:8px;margin-top:2px;flex-wrap:wrap}
  #fp-offer-{{ section.id }} .fp-tier__total{font-weight:800;font-size:17px;color:var(--fp-accent)}
  #fp-offer-{{ section.id }} .fp-tier__unit{font-size:12px;color:#888}
  #fp-offer-{{ section.id }} .fp-tier__perday{font-size:12px;color:#999}
  #fp-offer-{{ section.id }} .fp-tier__badge{position:absolute;top:-11px;left:16px;background:var(--fp-accent);color:#fff;font-size:11px;font-weight:700;padding:3px 10px;border-radius:20px;letter-spacing:.3px}
  #fp-offer-{{ section.id }} .fp-tier__save{flex:0 0 auto;background:var(--fp-accent-soft);color:var(--fp-accent);font-weight:700;font-size:12px;padding:5px 9px;border-radius:8px;white-space:nowrap}
  #fp-offer-{{ section.id }} .fp-offer__actions{display:flex;flex-direction:column;gap:10px;margin-top:18px}
  #fp-offer-{{ section.id }} .fp-btn{border:0;border-radius:12px;padding:15px 18px;font-size:16px;font-weight:800;cursor:pointer;width:100%;transition:transform .05s,filter .15s}
  #fp-offer-{{ section.id }} .fp-btn:active{transform:translateY(1px)}
  #fp-offer-{{ section.id }} .fp-btn--order{background:var(--fp-accent);color:#fff}
  #fp-offer-{{ section.id }} .fp-btn--cart{background:#fff;color:#222;border:2px solid #ddd}
  #fp-offer-{{ section.id }} .fp-btn--submit{background:var(--fp-accent);color:#fff;margin-top:6px}
  #fp-offer-{{ section.id }} .fp-offer__reassure{text-align:center;color:#888;font-size:12px;margin:12px 0 0}
  #fp-offer-{{ section.id }} .fp-order{margin-top:16px;border:2px solid var(--fp-accent-soft);border-radius:14px;padding:16px;background:#fbfefc}
  #fp-offer-{{ section.id }} .fp-order__lead{margin:0 0 12px;font-size:14px;color:#444}
  #fp-offer-{{ section.id }} .fp-order__row{margin-bottom:10px}
  #fp-offer-{{ section.id }} .fp-order input{width:100%;padding:13px 14px;border:1.5px solid #d5d5d5;border-radius:10px;font-size:16px}
  #fp-offer-{{ section.id }} .fp-order input:focus{outline:none;border-color:var(--fp-accent)}
  #fp-offer-{{ section.id }} .fp-order__summary{font-size:14px;color:#333;margin:6px 0 4px;font-weight:600}
  #fp-offer-{{ section.id }} .fp-order__err{color:#c0392b;font-size:13px;margin:8px 0 0}
  #fp-offer-{{ section.id }} .fp-order__success{margin-top:16px;border:2px solid var(--fp-accent);border-radius:14px;padding:16px;background:var(--fp-accent-soft);display:flex;flex-direction:column;gap:4px;text-align:center}
</style>

<script>
(function(){
  var root = document.getElementById('fp-offer-{{ section.id }}');
  if(!root) return;
  var pixelId = root.dataset.pixel, endpoint = root.dataset.endpoint;
  var variantId = root.dataset.variant, contentId = root.dataset.contentId;
  var currency = root.dataset.currency || 'EUR';
  if (pixelId && typeof window.fbq === 'undefined') {
    !function(f,b,e,v,n,t,s){if(f.fbq)return;n=f.fbq=function(){n.callMethod?
    n.callMethod.apply(n,arguments):n.queue.push(arguments)};if(!f._fbq)f._fbq=n;
    n.push=n;n.loaded=!0;n.version='2.0';n.queue=[];t=b.createElement(e);t.async=!0;
    t.src=v;s=b.getElementsByTagName(e)[0];s.parentNode.insertBefore(t,s)}
    (window,document,'script','https://connect.facebook.net/en_US/fbevents.js');
    window.fbq('init', pixelId);
  }
  var radios = root.querySelectorAll('input[name="fp-tier-{{ section.id }}"]');
  var orderForm = root.querySelector('[data-order-form]');
  var summary = root.querySelector('[data-order-summary]');
  var errEl = root.querySelector('[data-order-err]');
  var successEl = root.querySelector('[data-order-success]');
  function selected(){ var r = root.querySelector('input[name="fp-tier-{{ section.id }}"]:checked'); return { packs: parseInt(r.dataset.packs,10), total: parseFloat(r.dataset.total) }; }
  function syncUI(){ radios.forEach(function(r){ r.closest('.fp-tier').classList.toggle('is-selected', r.checked); }); var s=selected(); if(summary){ summary.textContent = s.packs + (s.packs===1?' пакет':' пакета') + ' · ' + s.total.toFixed(2) + ' ' + currency + ' с доставка'; } }
  radios.forEach(function(r){ r.addEventListener('change', syncUI); }); syncUI();
  var addBtn = root.querySelector('[data-add-cart]');
  if(addBtn){ addBtn.addEventListener('click', function(){ var s=selected(); addBtn.disabled=true; var old=addBtn.textContent; addBtn.textContent='Добавяне…';
    fetch('/cart/add.js',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:variantId,quantity:s.packs})})
    .then(function(){ if(window.fbq){ window.fbq('track','AddToCart',{content_ids:[contentId],content_type:'product',value:s.total,currency:currency,num_items:s.packs}); } window.location.href='/cart'; })
    .catch(function(){ addBtn.disabled=false; addBtn.textContent=old; }); }); }
  var openBtn = root.querySelector('[data-open-order]');
  if(openBtn){ openBtn.addEventListener('click', function(){ orderForm.hidden=false; orderForm.scrollIntoView({behavior:'smooth',block:'center'}); var f=orderForm.querySelector('input[name="fp_name"]'); if(f) f.focus(); }); }
  if(orderForm){ orderForm.addEventListener('submit', function(e){ e.preventDefault(); errEl.hidden=true;
    var name=orderForm.querySelector('input[name="fp_name"]').value.trim();
    var phone=orderForm.querySelector('input[name="fp_phone"]').value.trim();
    var digits=phone.replace(/[^0-9]/g,'');
    if(name.length<2){ errEl.textContent='Моля, въведете име.'; errEl.hidden=false; return; }
    if(digits.length<6){ errEl.textContent='Моля, въведете валиден телефон.'; errEl.hidden=false; return; }
    var s=selected();
    var isTest=/\btest\b|\bтест\b|notrack/i.test(name);
    if(!isTest && window.fbq){ window.fbq('track','Purchase',{value:s.total,currency:currency,content_ids:[contentId],content_name:'FitPatches',content_type:'product',num_items:s.packs}); }
    var payload={name:name,phone:phone,packs:s.packs,total:s.total,currency:currency,product:contentId,variant_id:variantId,page:window.location.href,ts:Date.now()};
    if(endpoint){ try{ var blob=new Blob([JSON.stringify(payload)],{type:'text/plain;charset=UTF-8'}); if(navigator.sendBeacon){ navigator.sendBeacon(endpoint,blob); } else { fetch(endpoint,{method:'POST',body:JSON.stringify(payload),mode:'no-cors',keepalive:true}); } }catch(err){} }
    try{ fetch('/cart/add.js',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:variantId,quantity:s.packs,properties:{'Бърза поръчка':name+' / '+phone}}),keepalive:true}); }catch(err){}
    orderForm.hidden=true; successEl.hidden=false; successEl.scrollIntoView({behavior:'smooth',block:'center'});
  }); }
})();
</script>

{% schema %}
{
  "name": "FitPatches Оферта",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "pixel_id", "label": "Meta Pixel ID", "default": "1051819453965625" },
    { "type": "url", "id": "cloud_endpoint", "label": "Cloud endpoint за поръчката" }
  ],
  "presets": [ { "name": "FitPatches Оферта" } ]
}
{% endschema %}
